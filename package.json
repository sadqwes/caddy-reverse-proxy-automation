{
  "name": "caddy-reverse-proxy-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Generate and validate Caddy reverse proxy configs for multiple domains, with health checks and service monitoring.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate:config": "node scripts/validate-config.js",
    "generate": "node scripts/generate-caddyfile.js",
    "health-check": "node scripts/health-check.js"
  }
}
